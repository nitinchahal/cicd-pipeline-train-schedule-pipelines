pipeline{
  agent any
  stages{
    stage('Buid') {
      steps{
        echo 'Running Build autoamadfgsdatsion'
        sh' ./gradlew build --no-daemon'
        echo 'Build COmplete'
      }
    }
  }
}
